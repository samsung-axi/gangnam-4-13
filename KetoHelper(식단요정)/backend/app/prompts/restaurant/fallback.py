"""
Restaurant Agent 폴백 프롬프트들
프롬프트 파일 로딩이 실패했을 때 사용되는 최종 폴백
"""

# 검색 쿼리 개선 폴백 프롬프트
FALLBACK_SEARCH_IMPROVEMENT_PROMPT = """
사용자의 식당 검색 요청을 분석하여 검색 키워드를 생성하세요.

사용자 메시지: "{message}"

키토 식단에 적합한 식당 키워드를 쉼표로 구분하여 제시하세요.
예: "스테이크하우스", "구이 전문점", "샐러드 전문점"
"""

# 검색 실패 폴백 프롬프트
FALLBACK_SEARCH_FAILURE_PROMPT = """
식당 검색 결과가 없을 때 도움이 되는 대안을 제시하세요.

검색 요청: "{message}"

키토 식단에 적합한 일반적인 조언과 대안을 제공해주세요.
다른 지역 검색이나 직접 요리를 권해보세요.
"""

# 추천 생성 폴백 프롬프트
FALLBACK_RECOMMENDATION_PROMPT = """
검색된 식당들을 바탕으로 기본 추천을 생성하세요.

사용자 요청: "{message}"
식당 목록: {restaurants}
사용자 프로필: {profile}

키토 관점에서 식당의 장점과 주문 팁을 간단히 제공해주세요.
"""

# 장소 검색 개선 폴백 프롬프트  
FALLBACK_PLACE_SEARCH_IMPROVEMENT_PROMPT = """
"{message}"에서 키토 친화적 식당 검색 키워드를 추출하세요.

키토 식단에 적합한 키워드들을 제안해주세요:
- 고기 요리, 해산물, 샐러드 위주
- 탄수화물이 적은 메뉴 제공 가능한 곳
"""

# 장소 검색 실패 폴백 프롬프트
FALLBACK_PLACE_SEARCH_FAILURE_PROMPT = """
"{message}"에 대한 검색 결과가 없습니다.

키토 식단을 위한 대안 제안:
1. 다른 지역에서 검색해보세요
2. 일반 식당에서도 키토 메뉴 주문 가능
3. 직접 요리하는 것도 좋은 선택입니다

키토 외식 팁을 간단히 알려드릴게요.
"""
