FROM node:18

WORKDIR /app

# package.json 복사 및 의존성 설치
COPY package*.json ./
RUN npm install

# 소스 코드 복사
COPY . .

# 포트 명시
EXPOSE 5173

# 개발 서버 실행 (Vite의 경우)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]