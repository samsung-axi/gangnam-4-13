FROM nginx:alpine

# Copy frontend static files
COPY frontend/ /usr/share/nginx/html/

# Custom nginx config for Lightsail (localhost proxy)
COPY nginx.lightsail.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
