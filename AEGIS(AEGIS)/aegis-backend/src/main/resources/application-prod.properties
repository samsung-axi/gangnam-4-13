# ==================== AWS Production Profile ====================
# Activated by SPRING_PROFILES_ACTIVE=prod (set in ECS task definition)

# S3: Use AWS native (IAM Task Role) instead of MinIO
aws.s3.endpoint=
aws.s3.access-key=
aws.s3.secret-key=
